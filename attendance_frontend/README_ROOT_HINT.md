# Flutter Root Command Hints

Run the following commands from this directory:

- flutter pub get
- flutter analyze
- flutter test
- flutter run

If tools cannot auto-detect the root, set the working directory to:
attendance-tracker-216426-216435/attendance_frontend

Markers to assist detection:
- .flutter-project-root
- FLUTTER_PROJECT_ROOT.marker
- PROJECT_ROOT.marker
- project_root.json
