# Backend endpoints (adjust host for your environment)
# Android emulator -> host is 10.0.2.2
# Desktop/iOS simulator -> localhost is fine

API_BASE_URL=http://localhost:8000
REALTIME_URL=ws://localhost:8000/ws
EVENTS_URL=http://localhost:8000/events

# When true, the app may use local mock data paths instead of real backend
MOCK_MODE=false
